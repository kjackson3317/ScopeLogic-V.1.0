[
  {
    "before": "import DrawingTakeoffPage, { type DrawingAnnotation, type DrawingMeasurement, type DrawingPageCalibration, type DrawingTakeoffMark, type DrawingTakeoffTool } from './drawing-takeoff';\nimport SlrChildEditor from './slr-child-editor';\nimport { associatedClarificationNumbers, checklistChildrenForDeliverable, lockIssuesForOfficialRelease, normalizeLegacyChildren, normalizeProjectIssueNumbers, recommendBaseBidSummary, rfiChildrenForDeliverable, syncLegacyFields, type SlrChildFields } from './slr-model';\nimport {\n",
    "after": "import DrawingTakeoffPage, { type DrawingAnnotation, type DrawingMeasurement, type DrawingPageCalibration, type DrawingTakeoffMark, type DrawingTakeoffTool } from './drawing-takeoff';\nimport SlrChildEditor from './slr-child-editor';\nimport DemoProjectReviewPanel from './demo/project-review-panel';\nimport { isEmployerDemo, DEMO_WORKSPACE_KEY, DEMO_PROJECT_ID, DEMO_MASTER_ID } from '../lib/demo/config';\nimport { readDemoWorkspace, resetDemo } from '../lib/demo/client';\nimport { associatedClarificationNumbers, checklistChildrenForDeliverable, lockIssuesForOfficialRelease, normalizeLegacyChildren, normalizeProjectIssueNumbers, recommendBaseBidSummary, rfiChildrenForDeliverable, syncLegacyFields, type SlrChildFields } from './slr-model';\nimport {\n"
  },
  {
    "before": "\nasync function readStoredFile(key: string) {\n  const database = await openFileDatabase();\n  const result = await new Promise<Blob | null>((resolve, reject) => {\n",
    "after": "\nasync function readStoredFile(key: string) {\n  if (isEmployerDemo && key === `${DEMO_PROJECT_ID}:demo-drawings`) {\n    const response = await fetch('/demo/Municipal-Technology-Plans.pdf');\n    if (!response.ok) throw new Error('Demo drawing could not be loaded.');\n    return response.blob();\n  }\n  const database = await openFileDatabase();\n  const result = await new Promise<Blob | null>((resolve, reject) => {\n"
  },
  {
    "before": "\n  const refreshMasterProjects = useCallback(async () => {\n    setMasterLoadError('');\n    const [masterResult, engagementResult] = await Promise.all([\n",
    "after": "\n  const refreshMasterProjects = useCallback(async () => {\n    if(isEmployerDemo)return;\n    setMasterLoadError('');\n    const [masterResult, engagementResult] = await Promise.all([\n"
  },
  {
    "before": "    let active = true;\n    const localSnapshot = readLocalWorkspace();\n    const localMeta = readLocalSyncMeta();\n    if (localSnapshot) applySnapshot(localSnapshot);\n",
    "after": "    let active = true;\n    const localSnapshot = readLocalWorkspace();\n    if (isEmployerDemo) {\n      const seed = readDemoWorkspace();\n      applySnapshot(seed);\n      setMasterProjects([{id:DEMO_MASTER_ID,projectNumber:'MP-26001',name:seed.projects[0].name,location:'Example City',status:'Bidding',revision:'Rev 2',systems:seed.projects[0].systems,createdAt:'2026-09-16',updatedAt:'2026-09-16',isArchived:false,engagements:[{id:'demo-engagement',legacyId:DEMO_PROJECT_ID,clientName:'Municipal Facilities Department',engagementType:'Preconstruction',engagementLabel:'Technology Estimate'}]}]);\n      setDataMode('local-fallback');setSyncState('synced');setHydrated(true);\n      return;\n    }\n    const localMeta = readLocalSyncMeta();\n    if (localSnapshot) applySnapshot(localSnapshot);\n"
  },
  {
    "before": "  useEffect(() => {\n    let active = true;\n    if (!hydrated || dataMode !== 'cloud') {\n      setOfficialReleases([]);\n      return () => { active = false; };\n",
    "after": "  useEffect(() => {\n    let active = true;\n    if (!hydrated || (!isEmployerDemo && dataMode !== 'cloud')) {\n      setOfficialReleases([]);\n      return () => { active = false; };\n"
  },
  {
    "before": "  useEffect(() => {\n    if (!hydrated) return;\n    localStorage.setItem('scopelogic-r14-8', JSON.stringify(cloudSnapshot));\n  }, [hydrated, cloudSnapshot]);\n\n",
    "after": "  useEffect(() => {\n    if (!hydrated) return;\n    localStorage.setItem(isEmployerDemo ? DEMO_WORKSPACE_KEY : 'scopelogic-r14-8', JSON.stringify(cloudSnapshot));\n  }, [hydrated, cloudSnapshot]);\n\n"
  },
  {
    "before": "  };\n\n  const openMasterProjectManager = () => { window.location.href = '/master-projects'; };\n\n  const retryCloudSync = async () => {\n    setSyncState('saving');\n    setSyncError('');\n",
    "after": "  };\n\n  const openMasterProjectManager = () => { if (isEmployerDemo) { setView('projects'); return; } window.location.href = '/master-projects'; };\n\n  const retryCloudSync = async () => {\n    if(isEmployerDemo){message('Local Demo', 'Changes are saved in this browser.');return;}\n    setSyncState('saving');\n    setSyncError('');\n"
  },
  {
    "before": "    try {\n      if (!kinds.length) return message('Select Deliverables', 'Choose at least one deliverable for the official release.');\n      if (dataMode !== 'cloud' || syncState !== 'synced') return message('Cloud Sync Required', 'Official releases can only be created while the production workspace is Cloud synced.');\n      const plannedReleaseNumber = await getNextOfficialReleaseNumber(projectId);\n      const lockedIssues = lockIssuesForOfficialRelease(issues, kinds) as Issue[];\n",
    "after": "    try {\n      if (!kinds.length) return message('Select Deliverables', 'Choose at least one deliverable for the official release.');\n      if (!isEmployerDemo && (dataMode !== 'cloud' || syncState !== 'synced')) return message('Cloud Sync Required', 'Official releases can only be created while the production workspace is Cloud synced.');\n      const plannedReleaseNumber = await getNextOfficialReleaseNumber(projectId);\n      const lockedIssues = lockIssuesForOfficialRelease(issues, kinds) as Issue[];\n"
  },
  {
    "before": "      setTimeout(() => URL.revokeObjectURL(url), 3000);\n      setOfficialReleases(await listOfficialReleases(projectId));\n      message(lockSyncWarning ? 'Official Release Created — Sync Required' : 'Official Release Created', lockSyncWarning ? `Release ${String(archived.releaseNumber).padStart(3, '0')} was archived and downloaded, but its customer-visible numbering locks still need cloud synchronization: ${lockSyncWarning}` : `Release ${String(archived.releaseNumber).padStart(3, '0')} was archived as an immutable cloud record, its numbering locks were persisted, and the PDF was downloaded.`);\n    } catch (error) {\n      message('Official Release Failed', error instanceof Error ? error.message : 'The combined PDF package could not be generated.');\n",
    "after": "      setTimeout(() => URL.revokeObjectURL(url), 3000);\n      setOfficialReleases(await listOfficialReleases(projectId));\n      message(lockSyncWarning ? 'Official Release Created — Sync Required' : 'Official Release Created', lockSyncWarning ? `Release ${String(archived.releaseNumber).padStart(3, '0')} was archived and downloaded, but its customer-visible numbering locks still need cloud synchronization: ${lockSyncWarning}` : `Release ${String(archived.releaseNumber).padStart(3, '0')} was archived, its numbering locks were persisted, and the PDF was downloaded.`);\n    } catch (error) {\n      message('Official Release Failed', error instanceof Error ? error.message : 'The combined PDF package could not be generated.');\n"
  },
  {
    "before": "  };\n\n  const syncLabel = dataMode === 'loading'\n    ? 'Cloud loading'\n    : dataMode === 'local-fallback'\n",
    "after": "  };\n\n  const syncLabel = isEmployerDemo ? 'Saved in this browser · Fictional demo' : dataMode === 'loading'\n    ? 'Cloud loading'\n    : dataMode === 'local-fallback'\n"
  },
  {
    "before": "        <Nav label=\"PROJECT\" items={[[\"projects\", \"Project Library\"], [\"calendar\", \"Calendar\"], [\"setup\", \"Project Setup\"], [\"dashboard\", \"Dashboard\"], [\"documents\", \"Project Documents\"], [\"notes\", \"Internal Notes\"], [\"internal\", \"ScopeLogic Internal Matrix\"]]} view={view} setView={navigateTo} />\n        <Nav label=\"DELIVERABLES\" items={navDeliverables} view={view} setView={navigateTo} />\n        <Nav label=\"ESTIMATING\" items={[[\"quotes\", \"Quote Builder\"], [\"quote-templates\", \"Quote Templates\"], [\"drawing-takeoff\", \"Drawing Take Off\"], [\"takeoff\", \"Take Off Rules\"], [\"scope-work\", \"Scope of Work\"], [\"parts\", \"Parts Database\"], [\"labor\", \"Labor & Pricing\"]]} view={view} setView={navigateTo} />\n        <Nav label=\"PROJECT CONTROL\" items={[[\"releases\", \"Official Releases\"], [\"exports\", \"Export Log\"], [\"contract\", \"Contract Information\"]]} view={view} setView={navigateTo} />\n        <Nav label=\"ADMINISTRATION\" items={[[\"customers\", \"Customer Database\"], [\"standards\", \"ScopeLogic Help\"], [\"production\", \"System Status\"]]} view={view} setView={navigateTo} />\n        <div className=\"sidebar-account\"><span>ACCOUNT</span><div className=\"sidebar-user\" title={userEmail}>{userEmail}</div><form action=\"/auth/signout\" method=\"post\"><button type=\"submit\">Sign Out</button></form></div>\n      </aside>\n      <main className=\"main\">\n",
    "after": "        <Nav label=\"PROJECT\" items={[[\"projects\", \"Project Library\"], [\"calendar\", \"Calendar\"], [\"setup\", \"Project Setup\"], [\"dashboard\", \"Dashboard\"], [\"documents\", \"Project Documents\"], [\"notes\", \"Internal Notes + Project Review\"], [\"internal\", \"SLR Register\"]]} view={view} setView={navigateTo} />\n        <div className=\"nav-group\"><span>DELIVERABLES</span><a href=\"/demo/deliverables\">Deliverables Workspace</a></div>\n        <Nav label=\"ESTIMATING\" items={[[\"quotes\", \"Quote Builder\"], [\"quote-templates\", \"Quote Templates\"], [\"takeoff\", \"Take Off Rules\"], [\"scope-work\", \"Scope of Work\"], [\"parts\", \"Parts Database\"], [\"labor\", \"Labor & Pricing\"]]} view={view} setView={navigateTo} />\n        <Nav label=\"PROJECT CONTROL\" items={[[\"releases\", \"Official Releases\"], [\"exports\", \"Export Log\"], [\"contract\", \"Contract Information\"]]} view={view} setView={navigateTo} />\n        <Nav label=\"ADMINISTRATION\" items={[[\"customers\", \"Customer Database\"], [\"standards\", \"Workspace Help\"], [\"production\", \"System Status\"]]} view={view} setView={navigateTo} />\n        <div className=\"sidebar-account\"><span>DEMO</span><div className=\"sidebar-user\" title={userEmail}>{userEmail}</div><span>Browser-local presentation</span><button type=\"button\" onClick={() => confirmAction('Reset Demo', 'Replace this browser’s demo edits with the fictional starting project?', resetDemo, 'Reset Demo')}>Reset Demo</button></div>\n      </aside>\n      <main className=\"main\">\n"
  },
  {
    "before": "function InternalNotes({ value, save }: { value: string; save: (value: string) => void }) {\n  const [draft, setDraft] = useState(value);\n  useEffect(() => setDraft(value), [value]);\n  return <><PageHead eyebrow=\"Internal Workspace\" title=\"Internal Notes\" description=\"Private project notes are stored with this project and are not included in client deliverables.\" action={<button className=\"primary\" onClick={() => save(draft)}>Save Notes</button>} /><div className=\"notes-page\"><RichTextEditor value={draft} onChange={setDraft} placeholder=\"Jot down project thoughts, follow-up items, coordination notes, and internal reminders...\" /></div></>;\n}\n",
    "after": "function InternalNotes({ value, save }: { value: string; save: (value: string) => void }) {\n  const [draft, setDraft] = useState(value);\n  useEffect(() => setDraft(value), [value]);\n  return <><PageHead eyebrow=\"Internal Workspace\" title=\"Internal Notes + Project Review\" description=\"Keep free-form project notes beside structured drawing/spec observations. Promote a review observation to an SLR only when it represents a scope risk.\" action={<button className=\"primary\" onClick={() => save(draft)}>Save Notes</button>} /><div className=\"demo-notes-review-grid\"><div className=\"notes-page demo-notes-column\"><RichTextEditor value={draft} onChange={setDraft} placeholder=\"Jot down project thoughts, follow-up items, coordination notes, and internal reminders...\" /></div><DemoProjectReviewPanel /></div></>;\n}\n"
  },
  {
    "before": "function AutoGrowTextArea({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { const ref = useRef<HTMLTextAreaElement>(null); useEffect(() => { if (!ref.current) return; ref.current.style.height = 'auto'; ref.current.style.height = `${Math.max(38, ref.current.scrollHeight)}px`; }, [value]); return <label className=\"field textarea auto-grow\"><span>{label}</span><textarea ref={ref} rows={1} value={value || ''} onChange={(event) => onChange(event.target.value)} /></label>; }\nfunction Check({ label, value, change }: { label: string; value: boolean; change: (value: boolean) => void }) { return <label><input type=\"checkbox\" checked={value} onChange={(event) => change(event.target.checked)} /><span>{label}</span></label>; }\n\n\n",
    "after": "function AutoGrowTextArea({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { const ref = useRef<HTMLTextAreaElement>(null); useEffect(() => { if (!ref.current) return; ref.current.style.height = 'auto'; ref.current.style.height = `${Math.max(38, ref.current.scrollHeight)}px`; }, [value]); return <label className=\"field textarea auto-grow\"><span>{label}</span><textarea ref={ref} rows={1} value={value || ''} onChange={(event) => onChange(event.target.value)} /></label>; }\nfunction Check({ label, value, change }: { label: string; value: boolean; change: (value: boolean) => void }) { return <label><input type=\"checkbox\" checked={value} onChange={(event) => change(event.target.checked)} /><span>{label}</span></label>; }\n"
  }
]
